get a list of all installed extensions on another machine:

```shell
$ code --list-extensions
```

take that list and install on another machine

```shell
code --install-extensionavidAnson.vscode-markdownlint
```
